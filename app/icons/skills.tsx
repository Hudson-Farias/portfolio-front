import { PythonIcon, TypescriptIcon, PhPIcon } from "@/portfolio-icons-lib";

import { SqlIcon, MongoIcon, RedisIcon } from "@/portfolio-icons-lib";
import { NextIcon, ReactIcon, TailwindIcon } from "@/portfolio-icons-lib";

import { NginxIcon, SwaggerIcon, PostmanIcon, DbeaverIcon } from "@/portfolio-icons-lib";
import { CodeIcon, GitIcon, DockerIcon, CloudIcon } from "@/portfolio-icons-lib";


export const skillIcons = {
    python: PythonIcon,
    typeScript: TypescriptIcon,
    php: PhPIcon,

    sql: SqlIcon,
    mongo: MongoIcon,
    redis: RedisIcon,

    nextjs: NextIcon,
    react: ReactIcon,
    tailwind: TailwindIcon,

    nginx: NginxIcon,
    swagger: SwaggerIcon,
    postman: PostmanIcon,
    dbeaver: DbeaverIcon,
    code: CodeIcon,
    git: GitIcon,
    docker: DockerIcon,
    cloud: CloudIcon,
} as const
